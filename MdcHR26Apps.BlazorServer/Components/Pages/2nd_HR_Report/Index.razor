@page "/2nd_hr_report"
@page "/2nd_hr_report/Index"
@using MdcHR26Apps.BlazorServer.Components.Pages.Components.Report.ViewPage

<PageTitle>부서장(팀장) 평가</PageTitle>

<h3>부서장(팀장) 평가</h3>
<hr />

@if (processTRLists == null)
{
    <p><em>Loading...</em></p>
}
else if (processTRLists.Count == 0)
{
    <p><em>No Data..</em></p>
}
else
{
    @* TODO: TeamLeaderViewExcel 컴포넌트 생성 필요 (Excel 다운로드 기능) *@
    @* <TeamLeaderViewExcel processTRLists="processTRLists" /> *@
    @* <hr /> *@
    <TeamLeaderReportListView processTRLists="processTRLists"></TeamLeaderReportListView>
}
